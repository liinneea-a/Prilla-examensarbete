// import ImageUpload from "../ImageUpload";
// import renderer from "react-test-renderer";

// describe("ImageUpload", async () => {
//   it("will upload an image", () => {
//     const imageUpload = renderer.create(<ImageUpload />);
//     const instance = imageUpload.root;
//     const button = instance.findByProps({ testID: "button" });
//     expect(button).toReturn("Upload Image");
//     expect(imageUpload).toMatchSnapshot();
//   });
// });
